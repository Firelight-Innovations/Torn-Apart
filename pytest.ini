[pytest]
# Default run excludes tests that require a Panda3D window / GPU.
# Run window tests explicitly with:  pytest -m window
addopts = -m "not window"

markers =
    window: marks tests that require a Panda3D window or GPU (deselected by default)
