# Optional developer / profiling tools — NOT runtime dependencies.
# Install with:  pip install -r requirements-dev.txt
-r requirements.txt

# Sampling profiler for Python hot-spots the named profiler scopes don't cover.
# Attaches to a running game by PID; emits speedscope JSON (opens in
# speedscope.app and the Perfetto UI) and `py-spy dump` for a stuck process.
# See docs/systems/profiler.md.
py-spy==0.4.0
